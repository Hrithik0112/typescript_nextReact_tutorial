export type PostProps = { id: number; title: string; body: string };
//if we are using same type object then we should make a separate file for every type and import them ,
//  or have general file like this for type that is used in our project
